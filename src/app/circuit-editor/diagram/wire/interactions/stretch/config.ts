// Tunables for the edge-stretch feature.

import { POSITION_TOLERANCE_PX } from '../../geometry';

/** Collinearity tolerance used when projecting a manual polyline onto moved endpoints. */
export const STRETCH_TOLERANCE_PX = POSITION_TOLERANCE_PX;
