// Tunables for the wire-reshape feature.

import { GRID_UNIT } from '../../../model/component-catalog';

/** Grid the reshape drag snaps dragged segments to. */
export const RESHAPE_SNAP_GRID_PX = GRID_UNIT;
