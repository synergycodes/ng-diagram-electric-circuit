// Wire-reshape feature: drag-handles on each orthogonal segment of a selected
// wire, with junction-aware propagation. Entry point: EdgeReshapeOverlayComponent.
// Depends only on wire/geometry and wire/junction — never on a sibling interaction.

export { EdgeReshapeOverlayComponent } from './edge-reshape-overlay.component';
export * from './config';
