// Link-drop-preview feature: a pulsing ghost dot shown while drawing a wire,
// marking where dropping on an existing wire would create a junction. Entry
// point: LinkDropPreviewOverlayComponent. Depends only on wire/geometry — never on a
// sibling interaction.

export { LinkDropPreviewOverlayComponent } from './link-drop-preview-overlay.component';
export * from './config';
